import React from 'react'

export const metadata: Metadata = {
  title: "충주라이브러리",
  description: "충주라이브러리",
};

const layout = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
